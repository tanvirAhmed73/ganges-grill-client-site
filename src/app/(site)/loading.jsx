import SitePageSkeleton from "@/components/ui/skeletons/SitePageSkeleton";

/** Shown during client navigations between marketing / shop routes */
export default function SiteSegmentLoading() {
  return <SitePageSkeleton />;
}
